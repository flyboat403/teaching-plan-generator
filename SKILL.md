---
name: teaching-plan-generator
description: >
  生成 40 分钟三段式课堂教案/教学设计（MD + DOCX 双输出），适配职业院校（中职/高职）两种
  学情（统一型/分层型）。当用户提到教案、教学设计、备课、公开课、课堂教学设计、分层教学、
  分层教案、教学流程设计、三段式教案时使用；也适用于用户提供课程内容文档、已有教案或企业岗位
  手册作为参考材料需要生成教案的场景。
---

# 三段10分钟课堂教学设计助手

## Overview

40分钟三段式教案生成工具，输出 MD + DOCX。支持手动输入和参考材料两种路径，自动根据学情选择统一或分层模板。

## 核心设计理念

在生成教案前，始终对齐以下三层逻辑：

1. **岗位导向**：这堂课学生将来在什么岗位场景下使用这个知识？开场案例必须来自真实企业。
2. **认知节奏**：40 分钟如何切割才能贴合职业院校学生的注意力曲线？严格遵循"筑基→攻坚→实操"三段递进。
3. **保底拔高**：最弱的学生必须带走什么？最强的学生还能吃到什么？分层教案必须明确标注基础/进阶。

这三点决定每一个字段的取舍。

## Required Inputs

在生成教案前，先收集以下 9 项参数。将已有信息和缺失项一次性展示，让教师补充：

| # | 参数 | 必填 | 示例 |
|---|------|------|------|
| 1 | 课程名称 | ✅ | 汽车发动机构造与维修 |
| 2 | 课题名称 | ✅ | 发动机怠速不稳的故障诊断 |
| 3 | 授课班级 | ✅ | 2025级汽修2班 |
| 4 | 授课人数 | ✅ | 35人 |
| 5 | 学段 | ✅ | 中职二年级 |
| 6 | 教育类型 | ✅ | 职业教育 |
| 7 | 使用教材 | ✅ | 《汽车发动机维修》第3版 |
| 8 | 教学课时 | ❌ | 默认 1课时 / 40分钟 |
| 9 | 授课类型 | ✅ | 理论新课 / 实操技能课 / 复习考核课 |

同时请教师描述学情（自然语言即可），用于后续学情类型判断。

## Decision: 学情类型判断

根据教师的学情描述判断使用哪个模板：

```
学情描述包含以下关键词之一：
  "分层" "混课时" "深度学习" "基础弱" "两极分化"
  "一部分...另一部分" "%" "差异大"
├─ 是 → 追问分层比例（如"60%基础 / 40%进阶"）→ 加载 references/layered-template.md
├─ 否 → 加载 references/uniform-template.md
└─ 无法判断（描述模糊或无学情描述）→ 询问：
    "班级学情更接近哪种？
     A. 学生水平基本一致，统一备课即可
     B. 存在明显分层，需要区分基础要求和进阶要求"
    → 选 A 加载 uniform-template.md，选 B 追问比例后加载 layered-template.md
```

## Workflow

### Step 0（可选）: 参考材料处理

如果教师提供了参考材料（课程文档/已有教案/企业手册），在收集参数前先执行材料解析。处理完成后展示预填结果，教师确认后进入 Step 1。

若无参考材料，直接进入 Step 1。

**材料来源处理**：
- 用户上传文件 → 保存到临时路径 → 识别扩展名 → 按格式解析
- 用户提供本地路径 → 直接读取文件 → 按格式解析
- 用户引用知识库（如 IMA）→ 读取知识库文档内容 → 按格式解析

详见 [Reference Material Processing](#reference-material-processing) 章节。

### Step 1: 收集/确认输入参数

收集 9 项参数。若有 Step 0 预填结果，仅展示未预填的必填字段要求补充。缺失必填项则追问补齐。

### Step 2: 判断学情 → 加载对应 template

按 [Decision: 学情类型判断](#decision-学情类型判断) 决策树执行。确定模板后：

**MANDATORY - READ ENTIRE FILE**：必须从头到尾完整读取选定的模板文件，**不要设置任何 range limit**。模板是字段结构、表格骨架和分层写法的唯一事实源，未完整读取前不得开始生成。

- `references/uniform-template.md` — 统一学情（4列表格，无分层标注列）
- `references/layered-template.md` — 分层学情（5列表格，含分层标注列）

**Do NOT Load**：未选中的另一个模板——只读一个，绝不两个同时加载。

读取完成后立即核对：模板中含 `{{占位符}}` 与 `{{#each}}...{{/each}}` 可重复区块——生成时逐一替换/展开，残留即不合格。

### Step 3: 生成教案

按 [Field Generation Rules](#field-generation-rules) 逐字段生成（字段数量与顺序以所加载模板为准）。教学流程严格遵循 [教学流程结构速览](#教学流程结构速览) 与所加载模板的逐分钟骨架。生成时先输出 MD 格式预览给教师确认。

### Step 4: 输出 MD + DOCX

教师确认后，输出两个文件：
- 文件名：`[课程名称]_[课题名称]_教学设计.md` 和 `.docx`
- DOCX 版式（页面/字体/表格/页脚页码/章节编号）以 `references/docx-style-spec.md` 为唯一事实源；JSON 结构与调用方式见 [Output Format Spec: DOCX](#output-format-spec)

**DOCX 生成校验**：
- 若 `python-docx` 未安装 → 先执行 `pip install python-docx` 再运行脚本
- 脚本执行后确认输出文件存在且大小 > 0；否则检查 JSON 是否符合 [Output Format Spec](#output-format-spec) 后重试
- 生成成功后立即删除临时 JSON 文件

### Step 5: 质量自检

生成后逐项执行 [Quality Checklist](#quality-checklist)，不符合则修复后重新输出。

---

## Reference Material Processing

当教师提供参考材料时，按以下流程处理：

### 材料类型识别

根据文件扩展名和内容特征判断材料类型：

| 类型代码 | 材料类型 | 典型特征 | 主要影响字段 |
|----------|---------|---------|-------------|
| A | 课程内容文档 | 含知识点/章节结构/学习目标 | 课程名称、课题名称、教材、教学目标、重难点 |
| B | 已有教案参考 | 含教学流程/方法/板书/评价 | 教学方法、学习方法、板书设计、评价设计、教学思路 |
| C | 企业岗位手册 | 含技能清单/工具清单/岗位案例 | 教学重点、开场案例、教学资源 |

### 格式解析

| 文件格式 | 解析方式 |
|----------|---------|
| PDF | 首选 `pypdf`；提取失败（扫描版/加密 PDF）时改用 `PyMuPDF`；仍失败则提示用户改用其他工具转文本 |
| DOCX | 使用 `python-docx` 提取文本 |
| MD / TXT | 直接读取 |
| 图片 (JPG/PNG) | `pytesseract`（需本地 Tesseract 引擎及中文包 chi_sim）+ `Pillow` 预处理；对低置信度（<70%）或识别明显异常（大量乱码/缺字）的文本标注提醒 |

**大文件分段读取**：参考材料较大时，每次只读取一部分（文本约 30-50 行 / PDF 若干页），按需定位关键章节，不要一次性读入全文，避免上下文溢出并确保信息完整取用。

### 信息提取与预填

从材料中提取可预填字段（课程名称、课题名称、教材名称、教学目标候选、重难点候选、教学资源候选），一次性展示给教师。

展示格式：
```
**从参考材料中提取的预填信息：**
- 课程名称：XXX（来源：材料A，文件名）
- 课题名称：XXX（来源：材料A，第3章标题）
- 教材：XXX（来源：材料A，前言）
- 教学目标候选：[列出提取到的学习目标/技能要求]
- 重难点候选：[列出提取到的重点/难点]

请确认或修改以上信息，确认后将继续补充授课班级、人数等参数。
```

### 多材料冲突处理

若多份材料对同一字段有不同信息：
1. 列出所有候选值，标注来源
2. AI 标注推荐项（★），给出推荐理由（如"来自课程内容文档，与课题直接相关"）
3. 教师选择/合并/自定义

---

## Field Generation Rules

按以下顺序逐字段生成，字段数量与顺序以所加载模板为准。

### 字段 1-9: 基础信息
直接填入教师确认后的值。第 8 教学课时默认 1课时/40分钟。

### 字段 10: 学情分析
基于教师描述的学情推断三项：

- **知识储备**：已学相关课程/知识基础
- **能力基础**：已掌握的技能/实操水平
- **学习风格**：学习习惯/偏好/常见问题

分层版额外注明："本班学情分层：基础学生约占 X%，进阶学生约占 Y%"

### 字段 11: 教学目标
使用 **ABCD 法 + SMART 原则**，对齐"保底拔高"理念——先写出最弱学生必须达到的下限，再向上拉伸上限。生成三项子目标：

- **知识目标**：学生能说出/列举/描述/解释...（可观测认知动词，禁止使用"了解""知道""掌握"等模糊动词）
- **技能目标**：学生能操作/完成/诊断/设计...（可观测行为动词）
- **素养目标**：培养/树立/增强...意识/习惯/态度

分层版额外拆分：
```
**基础目标**（全体学生达到）
  - 知识：...
  - 技能：...
  - 素养：...
**进阶目标**（深度学习学生达到）
  - 知识：...
  - 技能：...
  - 素养：...
```

### 字段 12-13: 教学方法与学习方法
基于授课类型自动推荐：

| 授课类型 | 推荐教学方法 | 推荐学习方法 |
|----------|-------------|-------------|
| 理论新课 | 案例教学法、讲授法 | 笔记整理、思维导图 |
| 实操技能课 | 任务驱动法、演示法 | 小组协作、实操练习 |
| 复习考核课 | 练习法、讨论法 | 自查互测、错题整理 |

若教师提供了已有教案参考（B 类材料），优先模仿其教学风格。

### 字段 14-15: 教学重点及解决措施 / 教学难点及突破措施
对齐"岗位导向"理念——每个重点之所以是重点，是因为它在岗位实操中最常用或出错代价最高；每个难点之所以是难点，是因为学生缺少对应的岗位经验。格式：

```
**教学重点**
1. XXX —— 解决措施：XXX
2. XXX —— 解决措施：XXX

**教学难点**
1. XXX —— 突破措施：XXX
2. XXX —— 突破措施：XXX
```

### 字段 16: 教学资源
按类别列出所需资源：

- 设备：...
- 工具/仪器：...
- 软件/平台：...
- 资料/文档：...

### 字段 17: 教学思路
阐述本课的教学理念与策略（偏理论层面，不与教学流程重复）。2-3 段，包含：教学理念选型、策略设计依据、与课程目标的关系。

### 字段 18: 教学流程
核心字段，严格按 [教学流程结构速览](#教学流程结构速览) 生成。对齐"认知节奏"理念——每个模块的时间分配贴合职业院校学生的注意力波峰/波谷（开场抓注意→中段攻坚→末段实操），不是随意切分。

### 字段 19: 评价设计
对齐"保底拔高"理念——评价不是一刀切的尺子，而是基础生和进阶生用不同的尺子量。生成评价量规表：

| 评价维度 | 评价指标 | 评价标准（分值） | 评价主体 | 评价方式 |
|----------|---------|-----------------|---------|---------|
| 学习态度 | （动态，如出勤、专注度、参与积极性） | （动态） | 教师/互评 | 观察/记录 |
| 学习行为 | （动态，如操作规范、协作配合、安全操作） | （动态） | 教师/学生互评 | 观察/记录/任务检查 |
| 学习效果 | （动态，如作品质量、测试成绩） | （动态） | 教师/企业导师 | 作品评分/测试 |
| 合计 | | 100分 | | |

- 至少包含学习态度、学习行为、学习效果三个维度
- 条目由 AI 根据课题动态增减（理实一体课增加"安全操作"指标等）
- 分层版含基础达标标准和进阶优秀标准

### 字段 20: 板书设计
按三部分结构生成：

```
**主板书面**：[核心知识框架/逻辑图/流程图，用缩进表示层级关系]
**副板书**：[关键词、公式、要点列表]
**布局示意**：[简述黑板分区，如"左上主板书，右侧副板书"]
```

### 字段 21: 教学反思
仅输出占位符：

```
（课后填写）
```

---

## 教学流程结构速览

### 总时间公式

```
10 (模块一) + 3 (缓冲一) + 10 (模块二) + 3 (缓冲二) + 10 (模块三) + 4 (终局复盘) = 40 min
```

### 六段结构

| 时段 | 段落 | 内容定位 |
|------|------|---------|
| 0-10min | 模块一：破冰筑基 | 导入锚定（真实企业场景）→ 1个基础概念/操作框架 → 小结+简单问题 |
| 10-13min | 缓冲一 | 不讲新知识。三选一：随机提问 / 行业小段子 / 快速复盘 |
| 13-23min | 模块二：重难点攻坚 | 衔接+岗位价值锚定 → 1个核心难点/易错点精讲 → 开放探究提问 |
| 23-26min | 缓冲二 | 不讲新知识。三选一：同桌互测 / 框架梳理 / 举手提问汇总 |
| 26-36min | 模块三：落地实操 | 知识串联 → 案例实操/错题拆解/技能模拟 → 基础/进阶要求梳理 |
| 36-40min | 终局复盘 | 1.5min 三模块核心梳理 + 1.5min 轻量课后任务 + 1min 留白答疑 |

**逐分钟表格骨架、环节内教师/学生活动与设计意图、分层标注的完整写法，以 Step 2 加载的模板文件为唯一事实源**（统一版 4 列 / 分层版 5 列）。本节只定义结构与约束，不重复模板内容。

### 内嵌表格规则

- 分层标注列：分层版显示，统一版隐藏
- 每行仅标注一级（全体 / 基础 / 进阶，不使用组合值）
- 若同一时间段内基础学生与进阶学生活动不同，拆为两行分别标注
- 核心授课环节可按需拆 2-6 行

### 教学流程约束（必须遵守）

- 每个 10 分钟模块只含 1 个核心知识点
- 模块一仅讲基础概念/操作框架，不延伸不拓展
- 模块二聚焦核心难点/易错点，不重复模块一内容
- 模块三做案例/实操/考点，不做新知识讲解
- 缓冲段不讲新知识
- 开场必须用真实行业场景/企业问题切入

## 绝对禁止（NEVER）

违反以下任一条将直接导致教案不合格：

- NEVER 在教学目标中使用"了解""知道""掌握"——它们不可观测、无法测量。必须替换为"说出""列举""操作""诊断"等行为动词。
- NEVER 在缓冲段（10-13min / 23-26min）讲授新知识——缓冲段的设计目的是认知休息和巩固，不是知识传递。
- NEVER 让模块一超出基础范畴——它只负责交付最低门槛，延伸和拓展留给模块二。
- NEVER 让模块三讲解新知识——第三模块的唯一用途是案例/实操/考点训练。
- NEVER 重写 `scripts/generate_docx.py`——始终通过 JSON 文件调用它。
- NEVER 用空泛导入开场（"今天我们学习..."）——必须使用真实行业场景、企业问题或岗位案例切入。
- NEVER 用泛化场景开场（如"某公司需要排版文档"）——导入案例必须具体到岗位/企业并能落到可操作动作，否则职业院校学生无法建立岗位连接，导入失去锚定作用。
- NEVER 让 `{{占位符}}` 残留在输出里——读取模板后必须全部替换为本课题内容，残留占位符直接判不合格。
- NEVER 让临时 JSON 文件滞留在仓库——生成 DOCX 成功后立即删除，防止下次生成误读陈旧内容。
- NEVER 交付缺要素的分层教案——基础/进阶双目标、流程表分层标注列、学情比例、双标准量规四要素缺一不可。
- NEVER 跳过质量自检——输出前必须逐项核对 Quality Checklist。

---

## Output Format Spec

### Markdown 输出

输出全部 21 个字段，按以下标题格式逐字段输出（完整字段列表参见模板文件；学情分析在教学目标之前）：

```
# 《[课程名称]》「[课题名称]」教学设计

## 一、课程名称      ## 八、教学课时       ## 十五、教学难点及突破措施
## 二、课题名称      ## 九、授课类型       ## 十六、教学资源
## 三、授课班级      ## 十、学情分析       ## 十七、教学思路
## 四、授课人数      ## 十一、教学目标     ## 十八、教学流程
## 五、学段          ## 十二、教学方法     ## 十九、评价设计
## 六、教育类型      ## 十三、学习方法     ## 二十、板书设计
## 七、使用教材      ## 十四、教学重点及解决措施  ## 二十一、教学反思
```

### DOCX 输出

使用 `python3 scripts/generate_docx.py <input.json> [output.docx]`（依赖 `python-docx`）。版式遵循 `references/docx-style-spec.md`（页面/字体/表格/页脚页码/章节编号的唯一事实源）。

**与 MD 的结构差异**：DOCX 不再把 1–9 项基础信息渲染为带编号的章节标题，而是先渲染一张**无编号的基础信息表**（9 行 × 2 列），其后为 **12 个带编号的 Heading 1 章节**（一 学情分析 → 十二 教学反思）。MD 仍为 21 字段编号结构（十 学情分析 / 十一 教学目标）。

**JSON 输入结构**：
```json
{
    "title": "《课程名称》「课题名称」教学设计",
    "basic_info": [
        ["课程名称", "办公软件应用"],
        ["课题名称", "文字环绕"],
        ["授课班级", "2025级计算机1班"],
        ["授课人数", "36"],
        ["学段", "中职一年级"],
        ["教育类型", "职业教育"],
        ["使用教材", "《Office 2019办公应用》"],
        ["教学课时", "1课时/40分钟"],
        ["授课类型", "实操技能课"]
    ],
    "sections": [
        {"heading": "一、学情分析", "content": "知识储备：..."},
        {"heading": "二、教学目标", "content": [
            {"text": "知识目标", "bold": true, "indent": false},
            {"text": "1. ...", "indent": true}
        ]},
        {"heading": "九、教学流程", "type": "tables", "content": [
            {"header": ["时间/环节", "教师活动", "学生活动", "设计意图"],
             "widths": [2.0, 6.4, 3.4, 3.2],
             "rows": [
                {"band": "【第一学时 0—40分钟】1.1 文字环绕"},
                {"band": "第一模块：破冰筑基（0—10分钟）—— ..."},
                ["0-2min 导入锚定", "...", "...", "..."],
                {"merged": "第一次缓冲（10—13分钟）：随机提问，不讲新知识"},
                {"band": "第二模块：重难点攻坚（13—23分钟）—— ..."},
                ["13-15min 衔接与价值锚定", "...", "...", "..."],
                {"merged": "第二次缓冲（23—26分钟）：同桌互测，不讲新知识"},
                {"band": "第三模块：落地实操（26—36分钟）—— ..."},
                ["26-28min 知识串联", "...", "...", "..."],
                ["36-40min 终局复盘", "梳理核心＋布置课后任务＋留白答疑", "...", "..."]
             ],
             "page_break_before": false}
        ]},
        {"heading": "十、评价设计", "type": "tables", "content": [
            {"header": ["评价维度", "评价指标", "评价标准（分值）", "评价主体", "评价方式"],
             "widths": [2.2, 3.4, 5.4, 2.2, 1.8],
             "rows": [
                ["学习态度", "...", "...", "教师/互评", "观察/记录"],
                {"cells": ["合计", "", "100分", "", ""], "merges": [[0, 1], [3, 4]], "total": true}
             ]}
        ]}
    ],
    "output_path": "output.docx"
}
```

**章节顺序（DOCX）**：`一、学情分析` → `二、教学目标` → `三、教学方法` → `四、学习方法` → `五、教学重点及解决措施` → `六、教学难点及突破措施` → `七、教学资源` → `八、教学思路` → `九、教学流程` → `十、评价设计` → `十一、板书设计` → `十二、教学反思`。

**content 类型推断**（`type` 可省略；显式 type 与内容形状矛盾时报错）：
- `type: "text"`（默认）：字符串，或 `[{"text","bold","indent"}]` 段落数组；`bold:true` 且未显式给 `indent` 时默认不缩进（知识目标/技能目标等小标签）
- `type: "tables"`：`[{"header","rows","widths"?,"page_break_before"?}]` 表格规格数组（多学时连堂 = 多个流程表规格）

**表格行类型（`rows` 数组）**：
- `["单元格1", ...]` —— 数据行：宋体 12pt 左对齐、无底纹、不设垂直居中
- `{"band": "整行文案"}` —— 分段带行：整行横向合并，黑体 12pt 加粗左对齐、底纹 EDEDED、垂直居中（学时/模块标题）
- `{"merged": "整行文案"}` —— 合并数据行：整行横向合并但按数据行样式（宋体 12pt、无底纹）；用于缓冲段与终局复盘
- `{"cells": [...], "merges": [[0,1],[3,4]], "total": true}` —— 合计行：按 ranges 合并，黑体 12pt 加粗居中、底纹 E7E7E7

**列宽 `widths`（cm，合计必须 = 15.0）**：流程 4 列 `[2.0, 6.4, 3.4, 3.2]`；分层流程 5 列 `[2.0, 5.6, 3.4, 2.6, 1.4]`；评价 5 列 `[2.2, 3.4, 5.4, 2.2, 1.8]`。省略时按章节标题与列数自动取上述默认值。`"page_break_before": true` 用于第二学时流程表强制另起一页。

**教学流程行结构（每学时 17 行）**：表头 → 学时 band → 模块一 band → 3 数据行 → 缓冲 merged 行 → 模块二 band → 3 数据行 → 缓冲 merged 行 → 模块三 band → 3 数据行 → 终局复盘 merged 行。时间列文案格式 `0-2min 导入锚定`（`min` 前无空格、半角连字符）。

**注意**：不要每次重新编写 DOCX 生成脚本。始终调用 `scripts/generate_docx.py`，通过 JSON 传入内容。

## Quality Checklist

生成教案后逐项自检，不符合则修复：

- [ ] 模板要求的全部字段无遗漏（统一版/分层版各 21 项），且 `{{占位符}}` 全部替换无残留
- [ ] MD/DOCX 中学情分析均在教学目标之前（十/学情分析 → 十一/教学目标；DOCX 一 → 二）
- [ ] 教学流程时间累加 = 10+3+10+3+10+4 = 40min
- [ ] 每个 10 分钟模块只有 1 个核心知识点
- [ ] 缓冲段（10-13min / 23-26min）不讲新知识
- [ ] 三个模块遵循「基础 → 难点 → 实操」递进梯度
- [ ] 开场使用具体到岗位/企业的真实场景（非泛化案例）
- [ ] 每个模块有小结或提问
- [ ] 教学目标使用可观测行为动词（无"了解""知道""掌握"）
- [ ] 教学方法推荐与授课类型匹配
- [ ] 分层版：教学目标拆分基础/进阶两级，学情分析注明比例，含分层标注列，评价量规含双标准
- [ ] DOCX 已成功生成且文件大小 > 0，版式符合 `references/docx-style-spec.md`（纯黑标题、页脚页码、表头底纹 D9D9D9、列宽合计 15.0cm），临时 JSON 文件已删除

## Template References

根据学情判断结果，只读取与决策结果匹配的一个模板（见 [Step 2](#step-2-判断学情--加载对应-template)）：

- **统一学情** → `references/uniform-template.md` — 4列教学流程表格，无分层标注
- **分层学情** → `references/layered-template.md` — 5列教学流程表格（多「分层标注」列），双目标/双标准

**使用方式**：读取模板后，将 `{{占位符}}` 全部替换为本次课题的具体内容（残留占位符判不合格）。`{{#each}}...{{/each}}` 表示可重复区块，按实际条目数量展开。

**Do NOT Load**（防过度加载）：
- 未选中的另一个模板——只读一个，不要同时加载两个
- `scripts/generate_docx.py` —— 仅按 [Output Format Spec](#output-format-spec) 构造 JSON 并调用；不要为生成而通读脚本实现（仅当 JSON 参数报错时才检查脚本）

## Dependencies

| 依赖 | 用途 | 安装命令 |
|------|------|----------|
| `python-docx` | DOCX 文件生成 + DOCX 参考材料读取（必需） | `pip install python-docx` |
| `pypdf` | PDF 参考材料文本提取（首选，纯 Python 轻量包） | `pip install pypdf` |
| `PyMuPDF` | PDF 提取备选，仅 pypdf 失败时按需安装 | `pip install PyMuPDF` |
| `pytesseract` | 图片格式参考材料 OCR 识别 | `pip install pytesseract`（需安装 Tesseract 引擎） |
| `Pillow` | 图片预处理（缩放、去噪） | `pip install Pillow` |
